Memory leak scan report
=======================

This report lists suspicious allocation or resource usage sites found by automated scanning.
Review each location and apply fixes: ensure matching DeleteObject/DeleteCriticalSection/CloseHandle/free/delete, or convert to RAII.

1) SparkUfsPdt/SparkLog/SparkLog.cpp
   - Background thread g_logThread started in SparkLog_Init and joined in SparkLog_Close. OK, ensure SparkLog_Close called on shutdown (CSparkUfsPdtDlg::OnDestroy calls SparkLog_Close).
   - g_logDir/g_logFile cleared in SparkLog_Close. OK.

2) SparkUfsPdt/SparkUfsPdtDlg.cpp
   - GDI objects created and deleted: m_pdtNameFont, m_pdtNameBrush, m_factoryComLinkedBrushGreen, m_factoryComLinkedBrushRed.
	 Ensure these are deleted in OnDestroy or destructor. Search for CreateFont/CreateSolidBrush/Create... -> see lines around OnInitDialog and OnDestroy.

3) SparkUfsPdt/ThreadPool/ThreadPool.h
   - ThreadPool uses worker threads; CSparkUfsPdtDlg::OnDestroy resets s_pool. Ensure ThreadPool destructor joins threads and frees resources.

4) SparkUfsPdt/CImpState.cpp
   - Several uses of new/delete in ConvertWCharDataToCharData / CharToWChar. Ensure all code paths delete[] tempBuf on error and success. These appear correct but should be converted to std::vector<char>/std::unique_ptr for safety.

5) SparkUfsPdt/SparkUfsPdt/CSerialPort.cpp
   - ReadThread loops and uses exceptions; ensure thread terminates and resources closed when Close() called. m_stRecvHead.NotifyWnd posted messages — ensure window valid on shutdown.

6) UFS_LIB/libsparkusb/* and libtfcard_mfc/*
   - Several legacy C APIs allocate buffers, open handles; manual review recommended.

7) General patterns to fix
   - Replace raw new[]/delete[] with std::vector or std::unique_ptr
   - Wrap GDI objects in RAII helpers that call DeleteObject in destructor
   - Ensure all InitializeCriticalSection have corresponding DeleteCriticalSection (g_logLock handled in OnDestroy; other critical sections should be checked)
   - Ensure threads are joined/stopped (ThreadPool destructor should join)

Suggested next steps:
 - I can create small patches converting the highest-risk sites (GDI brushes/fonts in the dialog, ConvertWCharDataToCharData allocations) to RAII. Approve and I will apply.

End of report
